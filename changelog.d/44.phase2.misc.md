Phase 2 of the by-design programme (#44): the follow-up Ask recorded by the stub now pins the Prior *block* at ≤240 characters (not only the 1000-char packed intent), and the R2 transport-error 502 envelope is asserted to contain no upstream scheme, host, or port (#58). Stale deferred comments that still claimed Prior packing and ask-log were blocked on #14/#43/#25 are brought in line with shipped `main`.
