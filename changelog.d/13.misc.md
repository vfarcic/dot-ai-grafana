Show-me navigation Asks now fail loudly instead of claiming Grafana evidence they do not hold: the 0-hop path reports an explicit failure when "Send Grafana evidence" is off, and now also whenever no Grafana evidence lines were found in the window — even when Map links are shown, since those links come from datasource configuration rather than from what was actually found. The failure summary says plainly that there is no evidence in the window and that the Map links merely open Explore for further digging. A failed Grafana stack read no longer leaves the previous Ask's drilldown links on screen alongside the new answer. The `hops` request field keeps its original meaning (the planned hop cap), distinct from `hop` (the current hop).
