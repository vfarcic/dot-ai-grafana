Release workflow now follows the dot-ai* convention: tag push plus `workflow_dispatch`, towncrier-assembled changelog, SHA-pinned actions, and the plugin version synced from the git tag so `plugin.json` no longer misreports itself.
